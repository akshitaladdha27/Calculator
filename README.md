# Calculator App (Vite + React)

This is a simple **Calculator application** built using **React** with **Vite** as the build tool.  
The project helped me understand core React concepts like **state management** and **event handling**.


## Project Setup

The project was created using Vite with the following command:

```bash
npm create vite@latest
